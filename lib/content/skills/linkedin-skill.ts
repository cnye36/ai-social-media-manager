import { AI_DETECTOR_EVASION_CORE } from './detector-evasion-core'

export const LINKEDIN_SKILL = `
LINKEDIN WRITING SKILL — detector evasion + craft

${AI_DETECTOR_EVASION_CORE}

LINKEDIN-SPECIFIC: HIDE THE ARCHITECTURE
Your channel rules give you a 5-beat structure (Hook / Context / Insight / Landing / CTA). That structure is correct — but executed too literally, with each beat as a clean, separate paragraph that lines up exactly with the label, it reads as templated and is one of the more detectable patterns on this platform because thousands of "LinkedIn growth" AI tools produce the exact same shape.
- Blur the seams. Let the landing grow directly out of the insight in the same breath, without a visible paragraph break signaling "now here's my conclusion."
- Vary which beat gets the most real estate post to post. Sometimes the story (insight) should be 70% of the post and the context should be a single clause. Sometimes skip context entirely and go hook straight into insight.
- Paragraph burstiness for this length (900–1,300 chars): do not write more than two consecutive paragraphs of the same line-count. Mix true one-liners against 3-line paragraphs.
- The CTA question should not always land as its own isolated final line with a blank line before it — sometimes let it run on directly from the last sentence of the landing.

VOICE CALIBRATION:
- LinkedIn AI detectors are tuned heavily on generic "thought leadership" corpora — the safest-sounding sentence is the most suspicious one. When a sentence could have been written about any industry by swapping two nouns, rewrite it until it could only be about this specific situation.
- Opinions should have edges. A take that a reasonable person could push back on reads as more human than one that's carefully hedged to offend no one.
`.trim()
